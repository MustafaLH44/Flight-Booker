module.exports={
    format_date: (date) => {
        // Format date as MM/DD/YYYY
        return date.toLocaleDateString();
      },

      // flight amount property needed
      // flight price amount
      // flight distance?
      // Need to render user selection?
}
